syntax = "proto3";

package auth;

option go_package = "github.com/CrabStash/crab-stash-protofiles/auth/proto";

// Login
message LoginRequest {
  string email = 1; // @gotags: valid:"email"
  string passwd = 2; // @gotags: valid:"required"
}

message LoginResponse {
  string token = 1;
  string refresh = 2;
}

// Register
message RegisterRequest {
  string email = 1; // @gotags: valid:"email"
  string passwd = 2; // @gotags: valid:"alphanum"
  string firstName = 3; // @gotags: valid:"alpha,minstringlength(1)"
  string lastName = 4; // @gotags: valid:"alpha,minstringlength(1)"
}

message RegisterResponse {
  string status = 1;
  string response = 2;
}

// Validate
message ValidateRequest {
  string token = 1; // @gotags: valid:"required"
}

message ValidateResponse {
  string uuid = 1;
}

// Refresh
message RefreshRequest {
  string token = 1; // @gotags: valid:"required"
}

message RefreshResponse {
  string token = 1;
}

// Logout
message LogoutRequest {
  string token = 1; // @gotags: valid:"email"
  string refresh = 2; // @gotags: valid:"email"
}

message LogoutResponse {
  string status = 1;
  string response = 2;
}

// Service

service AuthService {
  rpc Login (LoginRequest) returns (LoginResponse);
  rpc Register (RegisterRequest) returns (RegisterResponse);
  rpc Validate (ValidateRequest) returns (ValidateResponse);
  rpc Refresh (RefreshRequest) returns (RefreshResponse);
  rpc Logout (LogoutRequest) returns (LogoutResponse);
}